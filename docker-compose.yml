version: '3.8'

services:
  admin-angular-app:
    build:
      context: .
      dockerfile: development.Dockerfile
    ports:
      - "4200:80"
    environment:
      - NODE_ENV=development
    restart: unless-stopped
