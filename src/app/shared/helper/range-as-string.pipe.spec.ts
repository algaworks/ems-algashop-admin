import { RangeAsStringPipe } from './range-as-string.pipe';

describe('RangeAsStringPipe', () => {
  it('create an instance', () => {
    const pipe = new RangeAsStringPipe();
    expect(pipe).toBeTruthy();
  });
});
